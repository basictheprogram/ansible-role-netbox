# Managed by Ansible — do not edit manually
# Role: ansible-role-netbox
# Host: {{ inventory_hostname }}
#
# Docker Compose project-level environment file.
# Provides the VERSION variable consumed by docker-compose.yml:
#   image: docker.io/netboxcommunity/netbox:${VERSION-<fallback>}
# Bump netbox_version in defaults/main.yml (or group_vars) to upgrade.

VERSION={{ netbox_version }}
